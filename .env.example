# Example environment configuration
# Copy to `.env` and adjust as needed.

POSTGRES_DB=ecommerce_db
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# Port exposed by the Next.js container / service
APP_PORT=3000