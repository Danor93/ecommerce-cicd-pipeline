# Kustomization entry point – lists every resource that makes up the e-commerce stack
# Keeping resources in one list makes applying and tearing down the app a single command
resources:
  - namespace.yaml
  - configmap.yaml
  - database-init-configmap.yaml
  - persistent-volume.yaml
  - postgres-deployment.yaml
  - nextjs-deployment.yaml
